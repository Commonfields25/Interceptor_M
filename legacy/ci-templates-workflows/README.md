# legacy/ci-templates-workflows/

Archived from `ci-templates/workflows/`. These files are duplicates of the active
workflows under `.github/workflows/`, which is the canonical location.

Archived files (identical copies):
- `docs-lint.yml`
- `go Governance.yml`
- `metrics.yml`
- `node24-validation.yml`
- `pr-triage.yml`
- `python-ci.yml`

Also found duplicate copies under `docs/ci-templates/workflows/` (governance.yml, metrics.yml,
pr-triage.yml) — those are also superseded by `.github/workflows/`.
