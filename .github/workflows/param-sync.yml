name: Parameter Synchronization Check

on:
  pull_request:
    branches: [main]
    paths:
      - 'PARAMETERS.json'
      - 'simulation/constants.py'

jobs:
  check-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332
      - name: Run Sync Check
        run: python3 scripts/param_sync_check.py
