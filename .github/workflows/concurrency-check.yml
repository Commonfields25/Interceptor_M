name: Agent Concurrency Coordination

on:
  pull_request:
    branches: [main]

jobs:
  check-locks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332
      - name: Validate Locks
        run: python3 scripts/lock_validator.py
